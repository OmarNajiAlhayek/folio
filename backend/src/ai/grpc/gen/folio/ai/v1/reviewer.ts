// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: folio/ai/v1/reviewer.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export interface ReviewerProfile {
  reviewerId: string;
  affiliation: string;
  reviewKeywords: string;
  displayName: string;
}

export interface ReviewHistoryRow {
  reviewerId: string;
  submissionId: string;
  abstract: string;
  keywords: string;
}

export interface SuggestReviewersRequest {
  queryText: string;
  limit?: number | undefined;
  candidateIds: string[];
  excludeReviewerIds: string[];
  indexProfiles: ReviewerProfile[];
  indexHistory: ReviewHistoryRow[];
  useCrossEncoder?: boolean | undefined;
}

export interface ReviewerSuggestionHit {
  reviewerId: string;
  finalScore: number;
  bioScore: number;
  historyScore: number;
  ceBioScore?: number | undefined;
  ceHistoryScore?: number | undefined;
  usedCrossEncoder: boolean;
}

export interface SuggestReviewersResponse {
  hits: ReviewerSuggestionHit[];
}

function createBaseReviewerProfile(): ReviewerProfile {
  return { reviewerId: "", affiliation: "", reviewKeywords: "", displayName: "" };
}

export const ReviewerProfile: MessageFns<ReviewerProfile> = {
  encode(message: ReviewerProfile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reviewerId !== "") {
      writer.uint32(10).string(message.reviewerId);
    }
    if (message.affiliation !== "") {
      writer.uint32(18).string(message.affiliation);
    }
    if (message.reviewKeywords !== "") {
      writer.uint32(26).string(message.reviewKeywords);
    }
    if (message.displayName !== "") {
      writer.uint32(34).string(message.displayName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReviewerProfile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReviewerProfile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reviewerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.affiliation = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reviewKeywords = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReviewerProfile {
    return {
      reviewerId: isSet(object.reviewerId)
        ? globalThis.String(object.reviewerId)
        : isSet(object.reviewer_id)
        ? globalThis.String(object.reviewer_id)
        : "",
      affiliation: isSet(object.affiliation) ? globalThis.String(object.affiliation) : "",
      reviewKeywords: isSet(object.reviewKeywords)
        ? globalThis.String(object.reviewKeywords)
        : isSet(object.review_keywords)
        ? globalThis.String(object.review_keywords)
        : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
    };
  },

  toJSON(message: ReviewerProfile): unknown {
    const obj: any = {};
    if (message.reviewerId !== "") {
      obj.reviewerId = message.reviewerId;
    }
    if (message.affiliation !== "") {
      obj.affiliation = message.affiliation;
    }
    if (message.reviewKeywords !== "") {
      obj.reviewKeywords = message.reviewKeywords;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReviewerProfile>, I>>(base?: I): ReviewerProfile {
    return ReviewerProfile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReviewerProfile>, I>>(object: I): ReviewerProfile {
    const message = createBaseReviewerProfile();
    message.reviewerId = object.reviewerId ?? "";
    message.affiliation = object.affiliation ?? "";
    message.reviewKeywords = object.reviewKeywords ?? "";
    message.displayName = object.displayName ?? "";
    return message;
  },
};

function createBaseReviewHistoryRow(): ReviewHistoryRow {
  return { reviewerId: "", submissionId: "", abstract: "", keywords: "" };
}

export const ReviewHistoryRow: MessageFns<ReviewHistoryRow> = {
  encode(message: ReviewHistoryRow, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reviewerId !== "") {
      writer.uint32(10).string(message.reviewerId);
    }
    if (message.submissionId !== "") {
      writer.uint32(18).string(message.submissionId);
    }
    if (message.abstract !== "") {
      writer.uint32(26).string(message.abstract);
    }
    if (message.keywords !== "") {
      writer.uint32(34).string(message.keywords);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReviewHistoryRow {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReviewHistoryRow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reviewerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.submissionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.abstract = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.keywords = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReviewHistoryRow {
    return {
      reviewerId: isSet(object.reviewerId)
        ? globalThis.String(object.reviewerId)
        : isSet(object.reviewer_id)
        ? globalThis.String(object.reviewer_id)
        : "",
      submissionId: isSet(object.submissionId)
        ? globalThis.String(object.submissionId)
        : isSet(object.submission_id)
        ? globalThis.String(object.submission_id)
        : "",
      abstract: isSet(object.abstract) ? globalThis.String(object.abstract) : "",
      keywords: isSet(object.keywords) ? globalThis.String(object.keywords) : "",
    };
  },

  toJSON(message: ReviewHistoryRow): unknown {
    const obj: any = {};
    if (message.reviewerId !== "") {
      obj.reviewerId = message.reviewerId;
    }
    if (message.submissionId !== "") {
      obj.submissionId = message.submissionId;
    }
    if (message.abstract !== "") {
      obj.abstract = message.abstract;
    }
    if (message.keywords !== "") {
      obj.keywords = message.keywords;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReviewHistoryRow>, I>>(base?: I): ReviewHistoryRow {
    return ReviewHistoryRow.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReviewHistoryRow>, I>>(object: I): ReviewHistoryRow {
    const message = createBaseReviewHistoryRow();
    message.reviewerId = object.reviewerId ?? "";
    message.submissionId = object.submissionId ?? "";
    message.abstract = object.abstract ?? "";
    message.keywords = object.keywords ?? "";
    return message;
  },
};

function createBaseSuggestReviewersRequest(): SuggestReviewersRequest {
  return {
    queryText: "",
    limit: undefined,
    candidateIds: [],
    excludeReviewerIds: [],
    indexProfiles: [],
    indexHistory: [],
    useCrossEncoder: undefined,
  };
}

export const SuggestReviewersRequest: MessageFns<SuggestReviewersRequest> = {
  encode(message: SuggestReviewersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.queryText !== "") {
      writer.uint32(10).string(message.queryText);
    }
    if (message.limit !== undefined) {
      writer.uint32(16).int32(message.limit);
    }
    for (const v of message.candidateIds) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.excludeReviewerIds) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.indexProfiles) {
      ReviewerProfile.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.indexHistory) {
      ReviewHistoryRow.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.useCrossEncoder !== undefined) {
      writer.uint32(56).bool(message.useCrossEncoder);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SuggestReviewersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSuggestReviewersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.queryText = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.candidateIds.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.excludeReviewerIds.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.indexProfiles.push(ReviewerProfile.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.indexHistory.push(ReviewHistoryRow.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.useCrossEncoder = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SuggestReviewersRequest {
    return {
      queryText: isSet(object.queryText)
        ? globalThis.String(object.queryText)
        : isSet(object.query_text)
        ? globalThis.String(object.query_text)
        : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : undefined,
      candidateIds: globalThis.Array.isArray(object?.candidateIds)
        ? object.candidateIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.candidate_ids)
        ? object.candidate_ids.map((e: any) => globalThis.String(e))
        : [],
      excludeReviewerIds: globalThis.Array.isArray(object?.excludeReviewerIds)
        ? object.excludeReviewerIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.exclude_reviewer_ids)
        ? object.exclude_reviewer_ids.map((e: any) => globalThis.String(e))
        : [],
      indexProfiles: globalThis.Array.isArray(object?.indexProfiles)
        ? object.indexProfiles.map((e: any) => ReviewerProfile.fromJSON(e))
        : globalThis.Array.isArray(object?.index_profiles)
        ? object.index_profiles.map((e: any) => ReviewerProfile.fromJSON(e))
        : [],
      indexHistory: globalThis.Array.isArray(object?.indexHistory)
        ? object.indexHistory.map((e: any) => ReviewHistoryRow.fromJSON(e))
        : globalThis.Array.isArray(object?.index_history)
        ? object.index_history.map((e: any) => ReviewHistoryRow.fromJSON(e))
        : [],
      useCrossEncoder: isSet(object.useCrossEncoder)
        ? globalThis.Boolean(object.useCrossEncoder)
        : isSet(object.use_cross_encoder)
        ? globalThis.Boolean(object.use_cross_encoder)
        : undefined,
    };
  },

  toJSON(message: SuggestReviewersRequest): unknown {
    const obj: any = {};
    if (message.queryText !== "") {
      obj.queryText = message.queryText;
    }
    if (message.limit !== undefined) {
      obj.limit = Math.round(message.limit);
    }
    if (message.candidateIds?.length) {
      obj.candidateIds = message.candidateIds;
    }
    if (message.excludeReviewerIds?.length) {
      obj.excludeReviewerIds = message.excludeReviewerIds;
    }
    if (message.indexProfiles?.length) {
      obj.indexProfiles = message.indexProfiles.map((e) => ReviewerProfile.toJSON(e));
    }
    if (message.indexHistory?.length) {
      obj.indexHistory = message.indexHistory.map((e) => ReviewHistoryRow.toJSON(e));
    }
    if (message.useCrossEncoder !== undefined) {
      obj.useCrossEncoder = message.useCrossEncoder;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SuggestReviewersRequest>, I>>(base?: I): SuggestReviewersRequest {
    return SuggestReviewersRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SuggestReviewersRequest>, I>>(object: I): SuggestReviewersRequest {
    const message = createBaseSuggestReviewersRequest();
    message.queryText = object.queryText ?? "";
    message.limit = object.limit ?? undefined;
    message.candidateIds = object.candidateIds?.map((e) => e) || [];
    message.excludeReviewerIds = object.excludeReviewerIds?.map((e) => e) || [];
    message.indexProfiles = object.indexProfiles?.map((e) => ReviewerProfile.fromPartial(e)) || [];
    message.indexHistory = object.indexHistory?.map((e) => ReviewHistoryRow.fromPartial(e)) || [];
    message.useCrossEncoder = object.useCrossEncoder ?? undefined;
    return message;
  },
};

function createBaseReviewerSuggestionHit(): ReviewerSuggestionHit {
  return {
    reviewerId: "",
    finalScore: 0,
    bioScore: 0,
    historyScore: 0,
    ceBioScore: undefined,
    ceHistoryScore: undefined,
    usedCrossEncoder: false,
  };
}

export const ReviewerSuggestionHit: MessageFns<ReviewerSuggestionHit> = {
  encode(message: ReviewerSuggestionHit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reviewerId !== "") {
      writer.uint32(10).string(message.reviewerId);
    }
    if (message.finalScore !== 0) {
      writer.uint32(17).double(message.finalScore);
    }
    if (message.bioScore !== 0) {
      writer.uint32(25).double(message.bioScore);
    }
    if (message.historyScore !== 0) {
      writer.uint32(33).double(message.historyScore);
    }
    if (message.ceBioScore !== undefined) {
      writer.uint32(41).double(message.ceBioScore);
    }
    if (message.ceHistoryScore !== undefined) {
      writer.uint32(49).double(message.ceHistoryScore);
    }
    if (message.usedCrossEncoder !== false) {
      writer.uint32(56).bool(message.usedCrossEncoder);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReviewerSuggestionHit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReviewerSuggestionHit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reviewerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.finalScore = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.bioScore = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.historyScore = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.ceBioScore = reader.double();
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.ceHistoryScore = reader.double();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.usedCrossEncoder = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReviewerSuggestionHit {
    return {
      reviewerId: isSet(object.reviewerId)
        ? globalThis.String(object.reviewerId)
        : isSet(object.reviewer_id)
        ? globalThis.String(object.reviewer_id)
        : "",
      finalScore: isSet(object.finalScore)
        ? globalThis.Number(object.finalScore)
        : isSet(object.final_score)
        ? globalThis.Number(object.final_score)
        : 0,
      bioScore: isSet(object.bioScore)
        ? globalThis.Number(object.bioScore)
        : isSet(object.bio_score)
        ? globalThis.Number(object.bio_score)
        : 0,
      historyScore: isSet(object.historyScore)
        ? globalThis.Number(object.historyScore)
        : isSet(object.history_score)
        ? globalThis.Number(object.history_score)
        : 0,
      ceBioScore: isSet(object.ceBioScore)
        ? globalThis.Number(object.ceBioScore)
        : isSet(object.ce_bio_score)
        ? globalThis.Number(object.ce_bio_score)
        : undefined,
      ceHistoryScore: isSet(object.ceHistoryScore)
        ? globalThis.Number(object.ceHistoryScore)
        : isSet(object.ce_history_score)
        ? globalThis.Number(object.ce_history_score)
        : undefined,
      usedCrossEncoder: isSet(object.usedCrossEncoder)
        ? globalThis.Boolean(object.usedCrossEncoder)
        : isSet(object.used_cross_encoder)
        ? globalThis.Boolean(object.used_cross_encoder)
        : false,
    };
  },

  toJSON(message: ReviewerSuggestionHit): unknown {
    const obj: any = {};
    if (message.reviewerId !== "") {
      obj.reviewerId = message.reviewerId;
    }
    if (message.finalScore !== 0) {
      obj.finalScore = message.finalScore;
    }
    if (message.bioScore !== 0) {
      obj.bioScore = message.bioScore;
    }
    if (message.historyScore !== 0) {
      obj.historyScore = message.historyScore;
    }
    if (message.ceBioScore !== undefined) {
      obj.ceBioScore = message.ceBioScore;
    }
    if (message.ceHistoryScore !== undefined) {
      obj.ceHistoryScore = message.ceHistoryScore;
    }
    if (message.usedCrossEncoder !== false) {
      obj.usedCrossEncoder = message.usedCrossEncoder;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReviewerSuggestionHit>, I>>(base?: I): ReviewerSuggestionHit {
    return ReviewerSuggestionHit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReviewerSuggestionHit>, I>>(object: I): ReviewerSuggestionHit {
    const message = createBaseReviewerSuggestionHit();
    message.reviewerId = object.reviewerId ?? "";
    message.finalScore = object.finalScore ?? 0;
    message.bioScore = object.bioScore ?? 0;
    message.historyScore = object.historyScore ?? 0;
    message.ceBioScore = object.ceBioScore ?? undefined;
    message.ceHistoryScore = object.ceHistoryScore ?? undefined;
    message.usedCrossEncoder = object.usedCrossEncoder ?? false;
    return message;
  },
};

function createBaseSuggestReviewersResponse(): SuggestReviewersResponse {
  return { hits: [] };
}

export const SuggestReviewersResponse: MessageFns<SuggestReviewersResponse> = {
  encode(message: SuggestReviewersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.hits) {
      ReviewerSuggestionHit.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SuggestReviewersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSuggestReviewersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hits.push(ReviewerSuggestionHit.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SuggestReviewersResponse {
    return {
      hits: globalThis.Array.isArray(object?.hits)
        ? object.hits.map((e: any) => ReviewerSuggestionHit.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SuggestReviewersResponse): unknown {
    const obj: any = {};
    if (message.hits?.length) {
      obj.hits = message.hits.map((e) => ReviewerSuggestionHit.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SuggestReviewersResponse>, I>>(base?: I): SuggestReviewersResponse {
    return SuggestReviewersResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SuggestReviewersResponse>, I>>(object: I): SuggestReviewersResponse {
    const message = createBaseSuggestReviewersResponse();
    message.hits = object.hits?.map((e) => ReviewerSuggestionHit.fromPartial(e)) || [];
    return message;
  },
};

/** ReviewerMatchingService indexes reviewer profiles/history and suggests reviewers for a query. */
export type ReviewerMatchingServiceService = typeof ReviewerMatchingServiceService;
export const ReviewerMatchingServiceService = {
  suggestReviewers: {
    path: "/folio.ai.v1.ReviewerMatchingService/SuggestReviewers" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SuggestReviewersRequest): Buffer =>
      Buffer.from(SuggestReviewersRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SuggestReviewersRequest => SuggestReviewersRequest.decode(value),
    responseSerialize: (value: SuggestReviewersResponse): Buffer =>
      Buffer.from(SuggestReviewersResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SuggestReviewersResponse => SuggestReviewersResponse.decode(value),
  },
} as const;

export interface ReviewerMatchingServiceServer extends UntypedServiceImplementation {
  suggestReviewers: handleUnaryCall<SuggestReviewersRequest, SuggestReviewersResponse>;
}

export interface ReviewerMatchingServiceClient extends Client {
  suggestReviewers(
    request: SuggestReviewersRequest,
    callback: (error: ServiceError | null, response: SuggestReviewersResponse) => void,
  ): ClientUnaryCall;
  suggestReviewers(
    request: SuggestReviewersRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SuggestReviewersResponse) => void,
  ): ClientUnaryCall;
  suggestReviewers(
    request: SuggestReviewersRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SuggestReviewersResponse) => void,
  ): ClientUnaryCall;
}

export const ReviewerMatchingServiceClient = makeGenericClientConstructor(
  ReviewerMatchingServiceService,
  "folio.ai.v1.ReviewerMatchingService",
) as unknown as {
  new (
    address: string,
    credentials: ChannelCredentials,
    options?: Partial<ClientOptions>,
  ): ReviewerMatchingServiceClient;
  service: typeof ReviewerMatchingServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
