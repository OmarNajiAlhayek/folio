/** OJS-aligned: anonymous = single-blind (reviewer hidden from author). */
export enum SubmissionReviewMethod {
  OPEN = 'open',
  ANONYMOUS = 'anonymous',
  DOUBLE_ANONYMOUS = 'double_anonymous',
}
