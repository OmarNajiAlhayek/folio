/** Redacted review row returned to the submission author (no confidential text, no recommendation). */
export type AuthorReviewPublicView = {
  id: string;
  commentsForAuthor: string;
  submittedAt: Date;
};
